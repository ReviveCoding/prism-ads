# Repository Instructions

`docs/MASTER_SPEC_V2.md` is the governing specification. Read it completely before substantive work.

On every fresh session, verify `PROJECT_STATE.json`, `HANDOFF.md`, the tail of `PHASE_LEDGER.jsonl`, the latest phase receipt, and material artifact hashes. Resume the first incomplete valid phase; do not restart completed expensive work.

Use Python 3.12 and Python-based HTTPS. Preserve protected-role isolation, sealed truth, frozen identities, append-only ledgers, atomic publication, and locked-output immutability. Never infer scientific validity from software success.
